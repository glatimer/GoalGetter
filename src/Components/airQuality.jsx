// airQulaity.jsx is a child component of weather.jsx that recieves data from the same fetch

const airQuality = ({ weatherData }) => {
  return (
    <div className="air-quality">
      <h2>Air Qaulity</h2>
      {/* create visual with data and display */}
    </div>
  );
};
